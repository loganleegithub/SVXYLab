# P7 只读审查记录

审查日期：2026-09-07。当前审查对象为P7，不是早先请求中的P4。审查依据为当前AGENTS.md、RESEARCH_SPEC.md、FEATURES.json、experiment.toml、TASKS.md及实际P7产物。

审查范围仅限数据真实性/覆盖、信息可得时间、标签成熟/重叠、训练变换前视、公司行动/基金制度、换仓/费用账本，以及预测/经济结论。没有重写系统、修改模型或新增治理要求。

审查时Git HEAD为`5cf576e0f45078d3c35a8a444106d000f5642f4d`，P7实现尚未提交；准确审查版本由[runs/p7/freeze.json](freeze.json)的49项源码清单固定，清单SHA256为`4e86b7908819e22788335b26ffc447415bc979f59c65ce97bcdfe408d39bc8ca`。历史产物为`data/clean/p7/20260907T041828788697Z/`，实际运行记录为[research_run.json](20260907T041828788697Z/research_run.json)。源码位置均指该冻结版本。

## 结论与阻断范围

P7历史评估未发现阻断性计算错误。日报有一项确定可复现的截止时钟错误，仅阻断“迟到不产生新预测”验收项；现有历史计算及已保存首条前向预报不受影响。另有一项可延期的模型一致性改进。审查不构成策略赚钱或资本使用认证。

### R1：日报在预测计算前检查截止并记录生成时间

- **分类：** 待修工程错误，范围仅为P7日报迟到门控；没有据此否定整段历史结果。
- **文件位置：** `src/svxylab/daily.py:201–202`取`finished`并检查截止，`:205–213`才计算三个模型的预测，`:214`将先前的`finished`写入`created_at_utc`，`:219`发布。
- **实际影响：** 若保存模型、推理等操作跨过决策截止，仍可能发布新预测，并将推理前的时间记作生成时间。实际首条预报于2026-09-07 04:21:49.997392 UTC生成，截止为次日19:00 UTC，未落入这个边界。
- **已运行反例：** 复用真实9月4日输入和9月模型，调用原`update_daily`；只把时钟和所有I/O替换成明确标记的内存夹具，无网络或落盘。截止检查为2026-09-08 18:59:59.900 UTC，推理完成为19:00:00.100，截止为19:00:00.000。原函数仍返回退出码0、`FORWARD_FORECAST_AVAILABLE`、`new_prediction_created=True`，记录的生成时间仍为18:59:59.900。
- **修正方向：** 全部预测数值计算完成后再取实际时间、检查截止并记录生成时间。本轮只记录，源码尚未修复。

### R2：Q90与P10独立预测存在同一分布下的一致性冲突

- **分类：** 可延期研究改进，不是时钟/账本计算错误。
- **文件位置：** `src/svxylab/models.py:160–169`独立拟合三个预测头；`data/clean/p7/20260907T041828788697Z/core/predictions.csv:451`为具体反例。
- **实际数值：** 2024-08-06决策的M2，信息日2024-08-05，模型`2024-08-01_M2`；Q90=`0.06596673181958766`，P10=`0.9999999998178788`，mu5=`-0.08224673960409676`。
- **最小复算：** 若Q90严格小于10%，同一分布的P(L5>=10%)应不超过10%；反方向也有对应限制。按1e-8边界容差检查667个评分日，M0 0日、M1 20日（13+7）、M2 43日（32+11）冲突。
- **实际影响：** 当天risk-only按Q90给出`0.05/Q90=0.7579578163239152`的仓位，同时P10显示接近100%的10%损失概率；main因mu5为负而保持现金。限制概率和分位的联合解释，但该仓位严格遵守原映射，P10没有进入持仓公式。
- **延期理由：** 原规格明确分别拟合三种目标，没有要求联合分布约束；不为本轮结果好看而修改冻结模型或重做评估。此限制不等于预测的实际结果已经有效。

## 可运行反例与独立复算

在项目根目录执行，以下两个审查脚本仅打印结果，不写项目文件、不下载数据、不发单：

```sh
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -B runs/p7/review_counterexamples.py
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -B runs/p7/review_independent_refit.py
```

第一个脚本复现原函数的R1截止错误和实际CSV中的R2冲突；其断言确认当前问题存在，不是“修复后应通过”的回归测试，不计入121项普通pytest。第二个独立重建训练成员和变换，以成熟库重新求解Logistic/Quantile，并用独立SVD方程求Ridge；不调用项目的拟合函数。

脚本保存后又实际运行一次，两者退出码均为0；输出完整记录在[review_script_results.json](review_script_results.json)。这里的退出码0表示反例/审查复算成功，不表示R1已修复。

已有完整P7独立审计可只读运行：

```sh
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -B - <<'PY'
from pathlib import Path
from svxylab.release import read_json
from svxylab.release_audit import validate_release
root = Path.cwd()
run = read_json(root / read_json(root/'runs/p7/latest_run.json')['run_record'])
print(validate_release(root, run['result']))
PY
```

## 实际核验结果

| 核验 | 实际结果 |
|---|---|
| 普通测试 | `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -B -m pytest -q -p no:cacheprovider`，121 passed in 3.95s |
| 版本/产物 | 49项冻结源码、910项P7产物摘要吻合；同时验证已有P1～P6输入/验收产物 |
| 原件与价格 | Yahoo/Cboe原件独立重建，两只ETF各1930日；价格字段最大差0，总回报指数最大差1.4779288903810084e-12 |
| 特征 | 全部21列共40530个位置；最大差1.2934098236883074e-14；核心完整1909日、含SKEW完整1905日 |
| 标签 | 1924行完整，6行未成熟；最大差9.71445146547012e-16；P7核心2016行模型/日期标签逐项核对 |
| 核心保存状态重建 | 2016行，最大差7.105427357601002e-15；162个候选验证损失 |
| 变体保存状态重建 | 7388行，最大差1.0658141036401503e-14；891个候选验证损失；M3独立Ridge系数最大差9.027500968983304e-14 |
| 额外独立重拟合 | 66份M1/M2月模型、198个预测头、1344行预测；最大原始/发布预测差7.105427357601002e-15；额外18组核心内层变换/成员核对 |
| 连续账本 | 264个账户、177672行逐笔独立方程通过；最大金额差4.598405212163925e-09美元 |
| 时间块抽样 | 1004000个标量、1004个区间复算通过，最大差0；保留完整日历、缺失日和配对关系 |
| 已有重跑产物比较 | 378份CSV、523份JSON仅排除真实计算时间后精确一致；264份账本逐字节相同、2份抽样索引相同 |
| 首条前向记录 | 接收<=生成<截止<执行；9个预测头复算最大差0；R5/L5/Y10全部为空 |
| 新增数据分支 | 只在内存将冻结日设为9月3日，用已存9月4日真实原件重放；19项新核心特征最大差7.771561172376096e-16，1929行旧核心值精确保留。无真实网络请求，不冒充新的前向下载验证 |
| 只读范围 | 审查前后7202个项目文件内容和mtime一致；排除.git、.venv、缓存及字节码目录。随后才按用户新指令写本记录 |

独立重拟合首次使用另一种等价列拼接次序，造成标准化浮点归约差约2.38e-13，Logistic原始分数差约6.77e-6，严格断言未通过。定位为审查脚本的数组拼接/归约顺序后，按被冻结的两步拼接次序重建，未修改项目源码或放宽断言，最终66份模型全量通过。只读到件/时钟夹具最初缺少虚拟路径摘要等I/O替代，修正夹具后完成；这些是审查夹具调试，不列为项目问题。完整重跑产物比较执行已有`compare_runs.py`时，唯一报告写入已拦截，原文件保持不变。

## 样本、经济结论与限制

2024-01-02至2026-09-04共672个核心预测日、667个评分日；24个阳性标签合并6个重叠区间。SKEW为670个预测日、665个评分日，缺失决策日2024-12-02、2024-12-09仍保留持仓损益。最后五个决策日（2026-08-31、09-01、09-02、09-03、09-04）的标签保持空，可执行预测继续进入截至冻结日的账本。

M2相对M0的MSE、对数损失、Pinball分别高47.21%、103.03%、12.40%；M2 main净收益-15.88%、最大回撤幅度54.03%，固定50%为+15.99%、24.84%。负结果、risk-only、挑战者及较差敏感性均保留。没有发现报告把这些结果夸大为模型优势。历史PIT仍是明确的次交易日可得假设，收盘与成本仍是执行代理；未来新交易日的真实下载尚未验证。

发行人/申报原文另核对：[2018年目标由-1x改为-0.5x](https://www.sec.gov/Archives/edgar/data/1415311/000119312518059052/d503117dex991.htm)，[2024-04-11两拆一](https://www.sec.gov/Archives/edgar/data/1415311/000119312524072839/d795086dex991.htm)。账本使用实际基金收益，没有再次扣基金层管理费。

## 后续授权与保存

审查结束后用户要求：“这些结果先记录，记录后提交推送”。因此将当前P7实现、历史运行记录和本次审查结果保存为待修检查点，推送既有origin/main；不把提交推送视为P7最后验收，不在本轮修复源码或改变模型。原始行情、data/clean派生数据及本地离线包继续留在忽略目录，不纳入提交。`reports/latest.html`保留为原交付快照，当前状态以STATUS.md和本记录为准。
