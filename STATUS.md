# 当前进度

更新日期：2026-09-07（Asia/Shanghai）。

状态：P2_ACCEPTED。用户已明确验收 P2 的完整、真实和可复算交付，不代表模型有收益优势。现保存 P2 本地检查点，随后仅执行 P3。此前 P1 检查点为 `dc3d4ff`，P0 为 `f3cc00f`。

## 工程完成 / 未完成

P2 工程已完成：使用既有 `.venv`、Python 3.12.13、arm64 macOS；一个 Python 包、一个实验配置、唯一命令入口和普通 pytest。增加总回报、交易日时钟、五日标签、连续账本与静态报告；在同一环境加入 Matplotlib 3.11.1，实际依赖锁定在 requirements-lock.txt。没有重建架构、系统安装、Web 服务、远端 Git 或交易账户连接。

实际运行：

```sh
.venv/bin/python -m svxylab baselines
.venv/bin/python -m svxylab baselines --open
```

两次均退出 0，内部普通 pytest **38 项通过、0 失败**，包含原有 18 项与本阶段 20 项；pip check 和 git diff --check 均成功。生成六个对照各 **1930 行**的连续账户，共 11580 行；标签 **1924 行完整、6 行尚未成熟**。

第二次运行的 11 份 CSV 与第一次逐字节相同，基准指标一致。P1 的 14 份已验收清洗文件和原始清单保持不变，141 条原始响应/导入记录摘要全部相符，没有重新下载行情。所有原始数据、P0/P1 失败记录以及本阶段两次结果保留。

真实账本除了逐日资产、股数、现金和成本对账，还用独立的 Decimal 40 位有符号成交额算式从 P1 输入重算了全部 11580 行；没有调用生产换仓函数。六个账户全部通过，最大期末资产差约 **1.99e-8 美元**。实际审计命令 `.venv/bin/python runs/p2/independent_recalculation.py` 退出 0，仅为本轮审计记录，日常复算仍用唯一项目入口。

报告 `reports/baselines.html` 已由 macOS 实际打开，浏览器清单确认 Chrome 页签“SVXYLab · P2 时钟与连续账本”。已查看 Matplotlib 真实净值/回撤图，HTML 本地文件链接全部存在。没有以 P1 当时的工具锁屏记录继续阻止用户已授权的阶段推进。

已实现现金、固定 25/50/75/100% 和 F2>F1 对照；未实现 P3 特征、联合预测、模型优化、日常预测或交易执行。

## 研究结果

本阶段产生的是**基于真实行情的研究模拟账户**与会计核对结果。主现金收益 0、研究资本 100000 美元、单边成本 5bp；旧持仓先承担执行日损益，再按下一交易日收盘的实际仓位换仓，目标定义为扣费后权重。固定权重再平衡也按实际成交额扣费；没有重复扣基金费用、叠加五日标签交易或假设末日清仓。

页面图与绩效表展示 **2019-01-02 至 2023-12-29，共 1258 日**；这个范围在计算前写入实验记录。2024 年后的全段账本保存为 CSV，页面仅作公司行动/时钟核对，模型封存评估仍在 P7。

| 预定对照 | 年化收益 | 最大收盘回撤 | 平均旧仓位 | 累计成本（美元） |
|---|---:|---:|---:|---:|
| 现金 | 0.00% | 0.00% | 0.00% | 0.00 |
| 固定 25% | 6.02% | -20.54% | 24.98% | 223.75 |
| 固定 50% | 11.44% | -37.39% | 49.96% | 327.14 |
| 固定 75% | 16.09% | -51.11% | 74.94% | 275.52 |
| 固定 100% | 19.83% | -62.19% | 99.92% | 49.98 |
| F2>F1 持有，否则现金 | 12.97% | -46.03% | 86.56% | 3660.91 |

这些是六个预定对照的描述，未训练模型、未挑选参数，不构成联合模型有优势的证据。模型、公式、窗口、时点、成本和指标定义保持规格值；experiment.toml 与 FEATURES.json 本轮未改。P2 实现约定记录在 runs/p2/experiment_record.json，研究规格补充了相同口径。

三段真实手算已列入报告：2019-07-01 至 07-05 的固定 50%（4 行）、2020 年首次转为现金的边界（5 行）、2024-04-11 拆分前后（4 行）。每行给出旧仓位、当日总回报、持有损益、实际成本、新仓位和期末资产。

## 真实数据覆盖与时间边界

沿用 P1 冻结范围 **2019-01-02 至 2026-09-04，1930 个股票交易日**。VIX/VVIX/VIX9D/VIX3M、VX 前三月度合约及 SVXY/SPY 核心共同样本完整；SKEW 缺 2019-07-05、2024-11-29 两日，不参与 P2。原始资料、来源、收到时间与摘要保留在 data/raw；未复用旧模型或因子结果。

SVXY/SPY 总回报从当时份额价格、拆分和现金分配计算；首日财富锚定 100，首日未知回报保持空。SVXY 的一次 2:1 拆分不制造 50% 亏损。SPY 的 30 次分红保留；主计算采用除息日收盘再投资代理，不冒充实际现金支付日期。本段 SVXY 没有报告现金分配。

SVXY 总回报与供应商复权参考只存在浮点级差异；SPY 最大单日差约 **2.520235bp**，发生在 2020-03-20。两种算法及原值保留在 returns CSV 和 runs/p2/return_reference_diagnostic.json；未覆盖原件、填平差异或为结果改算法。P1 的 Yahoo/Nasdaq/QC 价差和成交量差异仍按原记录保留。

标签按信息日 t、t+1 收盘入场、t+6 收盘结束。L5 是相对入场点的每日收盘最差损失，不是盘中或峰谷回撤；Y10 包含恰好 10% 的边界。最后一个完整标签的信息日是 **2026-08-27**，尾部 6 行保持空。分别保存结果发生/成熟时间与保守的次交易日截止可用时间；半日市、夏令时和节假日均按股票日历处理。历史 published_at 仍未知，只声明合理延迟假设，不声称完整历史 PIT。

## 交付位置与下一步

- 报告：reports/baselines.html；P0/P1 报告及失败记录保留。
- 最新运行：runs/p2/20260907T000150426421Z/baselines_run.json；同目录 pytest.xml。
- 每次 CSV/图：data/clean/p2/20260907T000150426421Z/，包括两只 ETF 总回报、clock、labels、六个 ledger 和 2019–2023 指标。
- 缓存复算：runs/p2/reproducibility.json。
- 独立真实账本复算：runs/p2/independent_recalculation.json（脚本同目录）。
- 最终文件、源码与浏览器确认：runs/p2/final_validation.json。
- 实际入口与测试命令：AGENTS.md；本轮原始命令：runs/p2/initial_invocation.json、final_invocation.json。

**唯一真正阻塞项：无。** P2 已验收，验收记录见 runs/p2/acceptance.json；按用户要求保存检查点后开始 P3。
