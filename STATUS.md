# 当前进度

更新日期：2026-09-07（Asia/Shanghai）。

状态：**P4_COMPLETE_AWAITING_ACCEPTANCE**。本轮先记录 P3 验收并保存本地 Git 检查点 **541178b**，再只执行 TASKS.md 的 P4。P4 尚待用户验收，未进入 P5。阶段验收只代表交付完整、结果真实且可复算，不代表模型有收益优势。

## 工程完成 / 未完成

**P4 工程完成。** 沿用一个 Python 包、一份实验配置、唯一命令入口、普通 pytest 和本地 CSV/JSON/HTML。复用 Python 3.12.13、现有 `.venv`，在其中安装 scikit-learn 1.9.0、SciPy 1.18.1 及所需依赖。macOS 26.6.2 / arm64 检查、PyPI 网络核实和二进制安装均有实际记录；没有系统安装、重建架构、连接 Git 远端或交易账户。

实际运行命令：

```sh
.venv/bin/python -m svxylab predictions --pilot
.venv/bin/python -m svxylab predictions
.venv/bin/python -m svxylab predictions --open
```

三次均退出 **0**，每次普通 pytest **83 项通过、0 失败**，包含此前 69 项和 P4 新增 14 项。首个合资格月试运行覆盖 14 个预测日，计算耗时 **0.888 秒**。首次完整计算 **10.670 秒**，最终复算 **10.763 秒**；这些时间不含绘图与测试，最终完整命令含报告、测试、打开共 **16.086 秒**。pip check、git diff --check 也成功。

实现 M0 历史常数、M1 的 19 项联合线性输入、M2 的 57 项二次样条基加 6 个预定交互；均值、损失分位、事件概率三个头分别拟合。标准化、节点和交互尺度只在当时训练样本拟合。月度重估共 **40 次 / 120 份模型快照**，年度时间块选参共 **4 次**。每次保留训练成员、特征/标签截止、行数和阳性/重叠事件数、有限网格全部候选、变换参数、系数、实际计算时间和历史模拟决策时间。

17 份 CSV、120 份模型 JSON、4 份年度选参 JSON 及真实预测图保存在最终时间戳目录；共 144 份产物有 SHA-256。已有试运行、首次完整运行和旧阶段产物均保留。未实现经济仓位映射、P5 净收益比较或封存段模型评价。

## 时间边界、测试与独立复算

首个合资格信息日 **2020-09-10**，决策日 **2020-09-11**，拟合截止为该日 **19:00 UTC（收盘前 60 分钟）**。训练信息日为 **2019-02-01 至 2020-09-01，共 400 行**；35 行阳性对应 7 个合并的重叠事件簇。此前 **2020-01-02 至 2020-09-10 的 175 个请求决策日没有预测**，逐日原因保留；未补入旧制度数据或降低门槛。

标签必须同时满足 `label_matures_at < cutoff`、`label_available_at <= cutoff`，特征也须已到假设可用时间。内层每块开始前清除跨边界/未成熟训练标签，标准化和样条重新拟合；按完整股票交易日日历划定 1260 日最大窗口及三个 63 日验证块，不按删除缺失后的行号压缩时间。

普通测试实际覆盖严格成熟边界、可用时点、训练窗口、内层清除、训练专用变换与节点、六个交互、外推、单类后备、预测投影和未来扰动。另一次真实输入上的**合成测试夹具**人为改变 2021-06-15 之后的 ETF、VX 和隐含指数，再重算特征、标签及完整滚动模型：此前 **576 行实际模型预测逐值相同**，更晚预测确实改变。该额外 pytest **1 项通过**；合成文件明确标为 SYNTHETIC_TEST_ONLY，保存在测试目录，未覆盖真实数据，不能作为行情或收益证据。

独立审计从已验收 P3 特征/P2 标签重新选择训练成员，用保存的节点、尺度和系数直接重建模型，不调用生产模型、时钟筛选或评分函数。最终核对 **120 份模型、14,958 个预测数值、216 个内层候选头、57 行时期指标**；训练成员/边界一致，预测最大绝对误差约 **1.29e-14**。命令 `.venv/bin/python runs/p4/independent_validation.py` 两次退出 0。

最终与首次完整复跑的 **17 份 CSV、120 份模型及 4 份年度选择的数值完全相同**；仅排除实际计算时间和耗时字段。首月试运行与完整运行相同日期的 567 行输出也完全一致，其中含明确的无预测行。比较脚本、字段排除清单和摘要保存在 runs/p4/reproducibility.json。模型算法在首次完整运行、实际未来扰动测试和最终复跑之间没有修改。

报告 **reports/predictions.html 已实际打开**：macOS 打开命令退出 0，浏览器清单确认 Chrome 页签“SVXYLab · P4 联合预测”。报告包含相对 M0 的损失、固定五组概率校准、分位覆盖、年度/连续季度、M1/M2 分歧、交互条件作用和超出训练范围情况。图为真实预测与后来成熟目标，没有账户净值或模拟收益图。

## 研究结果

可评分的共同决策样本为 **2020-09-11 至 2023-12-21，共 826 日**，出现 33 行 Y10=1，比例约 **4.00%**。五日标签相互重叠，不能把 33 行视作独立崩盘次数。全部模型保留，无按结果修改阈值、期限、窗口或参数网格。

| 模型 | 均值 MSE | 概率 Log loss | Brier | 分位 Pinball | Q90 覆盖率 |
|---|---:|---:|---:|---:|---:|
| M0 | 0.00188192 | 0.175922 | 0.039302 | 0.00758256 | 93.70% |
| M1 | 0.00202103 | 0.185673 | 0.041105 | 0.00803367 | 91.04% |
| M2 | 0.00213008 | 0.201349 | 0.041470 | 0.00760885 | 87.89% |

四种损失均越低越好。**当前开发段，M1/M2 的主要平均预测损失都高于 M0，未显示整体预测优势。** M1 的 MSE / Log loss / Pinball 相对 M0 分别高 7.39% / 5.54% / 5.95%；M2 分别高 13.19% / 14.45% / 0.35%。Q90 覆盖目标为 90%，覆盖率更高并不等于更优分位预测，需要结合 Pinball 看。尚未做显著性推断、成本后的仓位比较或收益优势验证。

概率较高组样本稀疏，空组照实保留。外层只有 M1 的 1 个 Q90 原始预测低于 0，按预定规则投影为 0，原值和标记均保存；其余模型无投影。M1/M2 各有同样的 **30 个预测日**至少一项特征超出当期训练最小/最大范围，未裁剪或删除；最大距离约 1.1024 个训练标准差。真实拟合没有收敛警告或单类后备，后备逻辑另有合成边界测试。

## 真实数据覆盖与限制

P1 冻结范围仍为 **2019-01-02 至 2026-09-04，共 1930 个股票交易日**；141 条原始保存记录、14 份 P1 清洗文件、13 份已验收 P2 产物和 16 份已验收 P3 产物摘要重新核对一致。原始下载、失败响应、来源差异和各阶段报告保留，未重新取数。P2 前置独立核账产生的新时间戳记录另存 P4，已验收 P2 命名记录恢复原字节，数值结果没有变化。

P4 仅读入 **2019-01-02 至 2023-12-29 的 1258 个信息日**，只用 19 项核心，不使用 SKEW 扩展。请求的 **1006 个决策日**中，831 日生成三个模型预测，175 日没有预测。末尾 **2023-12-22、26、27、28、29** 的五日目标跨入 2024 年；保留其预测，目标值在数值解析前置空，因此每模型 826 日可评分、5 日不评分。2024 年后的标签没有参与拟合、评分或调参。

历史精确 `published_at` **尚未取得**，继续使用 ASSUMED_NEXT_SESSION / NO_FULL_HISTORICAL_PIT_CLAIM。研究假设信息 t 于 t+1 收盘前 60 分钟可用，执行为 t+1 收盘；额外一交易日延迟敏感性仍留给预定 P5。P1 来源差异、P2 SPY 总回报方法差异及早期导入时间限制仍保留。

experiment.toml 与 FEATURES.json 和 P3 检查点逐字节相同。P4 细化的缩放、内层时间块、正则库实现、投影、校准分组及展示边界在拟合前写入 runs/p4/experiment_record.json，并补充到唯一研究规格 RESEARCH_SPEC.md。

## 交付与记录

- [预测报告](reports/predictions.html)：双击即可打开。
- [最终运行](runs/p4/20260907T013333116356Z/predictions_run.json)：命令、83 项测试、版本和全部摘要；[逐日预测](data/clean/p4/20260907T013333116356Z/predictions.csv)。
- [首次可预测条件](runs/p4/eligibility_preflight.json)、[拟合前实验记录](runs/p4/experiment_record.json)、[依赖检查](runs/p4/environment_before.json)、[网络与依赖验证](runs/p4/dependency_probe.json)、[安装记录](runs/p4/dependency_install.json)。
- [精确复跑比较](runs/p4/reproducibility.json)、[最终独立数值审计](runs/p4/20260907T013333116356Z/independent_validation.json)、[实际未来扰动测试](runs/p4/20260907T012156654955Z/real_future_perturbation.json)、[最终打开/链接/摘要检查](runs/p4/final_validation.json)。
- [P3 验收记录](runs/p3/acceptance.json)。P3 本地检查点 **541178b**；此前 P2 / P1 / P0 为 **5912f3b / dc3d4ff / f3cc00f**。P4 文件待验收后再保存下一检查点，无远端。

**本阶段真正阻塞项：无。** 模型未优于 M0 是保留的研究结果；历史发布时间尚未取得是已披露限制。停在 P4，等待用户阶段验收。
