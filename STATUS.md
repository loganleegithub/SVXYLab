# 当前进度

更新日期：2026-09-07（Asia/Shanghai）。状态：**P4_ACCEPTED_P5_AUTHORIZED**。用户已验收P4的完整性、可复算性与负结果保留；不代表认可模型有预测或投资优势。本轮仅继续P5。验收冻结见runs/p4/acceptance.json。

本次仅完成P4审查收尾，没有进入P5、新增模型、调参或改目标。开始审查时HEAD就是参考提交 **0810f1a5ae371b4a703b8c1d2e098ffb0c13eae6**，工作区干净，无新提交或既有差异需要合并；没有重置、丢弃已有工作或创建新的Git提交。当前未提交差异全部为本次审查代码、报告及记录。验收后才保存新的本地检查点。本地已配置origin，首次审查重跑日志即已存在；本次没有新增/修改远端，也没有执行fetch、push或上传。

## 工程执行：完成

复用Python 3.12.13及原`.venv`和依赖，没有安装新依赖、重建架构或下载行情。唯一项目入口增加P4审查选项：

```sh
.venv/bin/python -m svxylab predictions --review
```

该命令实际退出0；依次运行普通测试、原参数完整P4、保存系数独立审计、真实行情输入上的合成未来扰动，并在新目录追加诊断与导出本地离线包。完整P4计算10.791秒，831个预测日、175个无预测日、120份模型快照、4次年度选择；普通pytest **87项通过**（原83项及4项审查算术测试），另行真实未来扰动pytest **1项通过**。扰动夹具仍明确标为SYNTHETIC_TEST_ONLY，原件未被改变。

本次两次完整P4分别在`data/clean/p4/20260907T021038241650Z/`和`data/clean/p4/20260907T021724579078Z/`；最终运行记录为[runs/p4/20260907T021724579078Z/predictions_run.json](runs/p4/20260907T021724579078Z/predictions_run.json)。旧P4产物、原报告副本、旧审计和负结果全部保留。

当前汇总入口[runs/p4/review_latest.json](runs/p4/review_latest.json)指向[runs/p4/review_20260907T021720821796Z/](runs/p4/review_20260907T021720821796Z/)。其中保存实际命令/退出码、原始日志、XML、候选/系数/分组/逐日诊断及源版本摘要。修改范围为审查/导出模块、4项普通测试、入口开关、独立脚本离线路径支持及报告展示；`models.py`、`predictions.py`、`prediction_data.py`、`experiment.toml`、`FEATURES.json`与原运行一致。

## 复算结果：两种方法分别完成

**从原训练输入重新拟合：** 完整重新执行年度候选选择、训练期变换/样条和全部月度系数拟合。17份CSV与124份模型/选择JSON比较一致；只排除`computed_at_utc`和`elapsed_seconds`，绝对容差2e-11、相对容差2e-10。逐日三模型的6个原始/发布预测字段最大绝对差 **0**，缺失和历史决策日期也一致。

**按保存系数重建预测：** 独立审计从开发段真实特征/标签重建成员、训练尺度、样条和交互，再代入已保存系数。核对2493行模型预测、14958个预测值、216个候选头的已保存系数/验证损失、57行时期指标，预测最大绝对误差 **1.2878587085651816e-14**。这一步不重新优化系数，不能称独立重新拟合。

本次未发现真实计算错误，无最小反例、修复前后差异或受影响日期需要列示。没有把调模型/目标当成修bug。离线包另在项目目录之外解压，实际从包内开发段输入重新拟合、运行保存系数审计及87项普通测试，退出0；它不重建P1～P3原始行情链。

## 预测研究结果：原负结果不变

同826个可评分日期，M1相对M0的MSE/对数损失/Pinball分别 **+7.39% / +5.54% / +5.95%**，M2分别 **+13.19% / +14.45% / +0.35%**；主要平均预测损失仍没有优于M0。

- 4年×2模型×3个头的 **24次选择全部在最强收缩边界**：Ridge α=100、Quantile α=.01、Logistic C=.1。全部严格优于其余候选，数值并列0次；最小备选损失差3.374052898234771e-5，大于原并列阈值1e-12。报告展开全部72个候选的3块损失、加权损失及差距；未扩大网格。
- M2 Q90的I01～I06在 **40个月全部为精确零**，不是舍入；主效应仍有9～23个非零样条系数。其他头和每月逐项原系数均保留，没有取消正则化。
- 原等宽五组逐值保留；追加固定linear经验等频五组，同值概率不拆开，重复边界合并，保存边界/样本/阳性/平均概率/实际比例及逐日组成员。M2最低/最高等频组阳性为14/166与6/165，未呈现单调风险排序；没有校准器或交易阈值选择。
- **33个评分阳性行合并为11个重叠区间**，全部成员及区间列在报告。各模型最高概率日期和前10名（并列全留）也已列出。M1/M2最高均为决策日2021-01-28，概率46.71%/42.35%；随后R5=+9.71%、L5=2.31%、Y10=0。
- M2在 **2022Q1覆盖46/62=74.193548%，Q2为47/62=75.806452%**。已对原metrics核对，保存全部124日及31个未覆盖日的信息/决策/标签终点、原始/发布Q90和实际损失。

以上新增展示均明确标为**本次审查后追加诊断**，不称事先注册、不推导仓位，也不作统计显著性结论。

## 数据/PIT限制与真实覆盖

冻结P1核心共同样本1930日、冻结截止2026-09-04不变；P4只使用2019起的19项核心特征。首个预测信息日2020-09-10、决策日2020-09-11不变，未补入2019以前数据。开发段831个有预测日中826日可评分；跨2024的5个预测日期后续R5/L5/Y10仍为空。

历史精确`published_at`尚未取得；仍为ASSUMED_NEXT_SESSION / NO_FULL_HISTORICAL_PIT_CLAIM。P1来源差异、P2 SPY总回报差异、相邻五日标签依赖、早期缺乏预测和仅开发样本的限制不变。额外延迟敏感性留在P5，时间块不确定性分析留在P6。

## 本地离线包与交付

[中文P4报告](reports/predictions.html)复用原页面，开头追加四类审查说明和完整诊断。[本地离线包](data/clean/p4_review/review_20260907T021720821796Z/LOCAL_REVIEW_ONLY.zip)及[说明](data/clean/p4_review/review_20260907T021720821796Z/LOCAL_REVIEW_ONLY/README.md)位于原Git忽略的`data/clean/`下，没有上传或连接公开GitHub。

包内含当前源码/依赖/配置/字典、原与新P4预测/损失/候选/选择/成员/模型变换快照、测试与运行记录，以及仅开发段核心特征/可用性/标签。开发输入中6行跨2024标签结果为空，无封存信息日或封存结果。每份导出记录源SHA-256与导出SHA-256；历史源路径通过`source_references.json`映射到包内件或明确仅源工作区保留，页面可点链接只指向实际包内文件。

已有授权仅为个人非商业本地研究，没有取得行情或派生数据再分发授权。本包保留本地、不默认对外分享；P1～P3原件和真实行情扰动原料不随包导出，仍在源工作区核账。包不含虚拟环境或依赖二进制；其他离线机器需要已具备锁定的兼容环境。本机在解压目录执行`/Users/logan/SVXYLab/.venv/bin/python replay.py`，结果写新目录，不下载。

报告结构、数字、文件摘要及本地链接实际检查；本次浏览器工具的URL策略拒绝访问file地址，因此没有声称通过浏览器截图验收，也未绕过该限制。最终核验记录在本次审查目录。

**未解决的工程阻塞项：无。** 当前损失比较不变，数据/PIT与研究限制如上。P4现已由用户验收；接下来仅执行P5，验收不代表模型有收益优势。
