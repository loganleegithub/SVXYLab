# SVXYLab：给 Codex 的项目说明

最新确认（2026-09-10）：用户在E1交付后明确“提交推送”，本检查点记录E1交付确认与提交推送授权，见`runs/panic_retreat/acceptance.json`。按授权提交至现有origin/main，停在E1，不自动进入下一实验或前向监控。原报告、运行及下方交付记录保留当时状态；“等待验收/未提交推送”为历史状态。确认不代表收益优势，也不补全已披露的整页视觉检查。

当前交付（2026-09-10）：按用户附件完成E1恐慌退潮事件研究，等待验收；本轮不提交或推送，不开始下一实验。报告`reports/panic_retreat.html`，独立入口`.venv/bin/python -m svxylab.panic_retreat --open`，这是旧“唯一入口”的本次局部例外。冻结2019-01-02—2026-09-04真实1930日，主配置7轮/7簇；B的H10平均净收益+3.40%、最差−40.93%，连续账户+8.34%/最大回撤43.78%，再晚一日均值−0.04%。157项普通测试通过，50账户/96500行及319笔独立份额核算；两次28份CSV逐字节一致。已揭示历史，不修改M2/旧规格/旧入口/冻结链；整页截图被本地URL安全策略阻止，图表已直接检查。完整状态见STATUS.md和`runs/panic_retreat/final_validation.json`。旧D5以下验收记录保留，仅约束旧路线。

最新验收（2026-09-07）：用户明确“验收，提交推送”。D5已验收，记录`runs/m2_d5/acceptance.json`；本检查点保存D5实现和验收并按授权推送origin/main，包含此前已验收的D1—D4本地检查点6920be0。原报告和运行验证保留交付时状态；“等待验收/未推送”仅为下方历史记录。停在D5，负结果和已知限制继续保留，不自动启动第二个干预。

当前交付（2026-09-07）：D1—D4已验收并保存本地检查点6920be0；按原方案完成D5唯一收益头α100反证，报告`reports/falsification.html`，入口`.venv/bin/python -m svxylab falsification --open`或双击“M2有限反证.command”。141项普通测试通过，12账户/9030行核账；80份CSV、1335份计算JSON和全部12份账本两次逐字节一致。2024 MSE下降27.71%，已揭示净收益−15.88%→+3.08%，但最强事件敏感性上升、门槛切换/费用增加、尾部几乎不变，不能称策略获救。9533份旧产物不变，3项入口/规格兼容记录见`runs/m2_d5/runtime_compatibility.json`。D5等待验收；未提交D5、未推送、不自动试第二项。详见STATUS及`runs/m2_d5/final_validation.json`。

当前授权补充（2026-09-07）：用户明确“验收 D1—D4，按照方案执行 D5”。D1—D4验收见`runs/m2_mechanism/acceptance.json`；保存本地检查点后，只执行`runs/m2_mechanism/D5_proposal.md`中的唯一收益头α=100干预。D5后停止验收；不推送、不自动尝试第二个干预。原交付报告和验证记录保留当时状态。

当前进度补充（2026-09-07 M2机制诊断交付）：用户按附件授权M2机制归因，D1—D4已完成并等待验收；D5只提出一个收益头alpha固定100的方案，尚未执行，必须由用户单独决定。报告`reports/mechanism.html`，入口`.venv/bin/python -m svxylab mechanism --open`或双击“M2机制归因.command”。136项普通测试通过，132账户/99090行核账，73月原训练身份、1260事件簇固定/全变换影响诊断、146方差拟合完成；两个原时期、五偏移和全部正负结果保留。491份CSV/1417份JSON复算逐字节相同，全部132账本相同；另仅修正M0两项不适用标记。既有7589文件摘要不变，原日报交易逻辑/预测/数据不改；三个冻结文件的入口与规格兼容变动单独记录，不重冻结P7。内置浏览器禁止本地HTML，页面截图未核验；文件链接、JS语法及图表已检查。详见STATUS.md与runs/m2_mechanism/final_validation.json。本轮尚未验收，未提交推送；停在D1—D4，不自动执行D5或新策略。

当前进度补充（2026-09-07 N1交付）：用户授权修复已有反例的日报推理后截止门控并执行N1资本与经济基准。已从4caf0c0继续完成，报告`reports/capital.html`，入口`.venv/bin/python -m svxylab capital --open`或双击“资本与经济基准.command”。127项普通测试通过，82个账户/97078行独立核账，131份CSV两次完整计算逐字节相同；新增真实BIL1930日、70项分配。原P7历史结果、冻结与首条预报保留，5项源码/配置/规格兼容改动明确记录于`runs/n1/runtime_compatibility.json`，不是重冻结P7。N1属于2024—2026已揭示后的描述性研究；期权真实报价0行，许可与样本未验收。日报截止已修，Q90/P10一致性及真实新日下载验证仍待做。详见`STATUS.md`和`runs/n1/final_validation.json`。用户随后明确“提交推送 N1”，本检查点按该指令记录N1交付确认与提交推送授权，验收见`runs/n1/acceptance.json`；停在N1，不购买数据、不连接账户、不自动开始保护研究。报告与原验证记录保留交付时状态。

P7验收时的历史进度补充（2026-09-07，以下原记录保留）：用户明确“验收 P7，再次提交”，P7已验收，记录为 `runs/p7/acceptance.json`。实现及只读审查提交 `840a731334abf288403eb14e2ae06cd16bab6a8e` 已按上一条授权推送origin/main，本次保存本地验收检查点，停在P7。日报推理完成后缺少截止复查的问题仍未修复；Q90/P10一致性冲突仍属可延期研究改进。验收不意味着这些问题消失，不表示模型具有收益优势，也未授权本轮修复源码或推进新阶段。详见 `runs/p7/read_only_review_20260907.md` 和 `STATUS.md`；原运行/审查记录保留其当时状态。

用户是中文交易员，不会编程；本项目从零开始。此前 VVIX 等零散实验不是已验证因子库；不得默认复用 MatVIX、MatSHIX、Optimatrix 的方法或成果。

先读本文件、RESEARCH_SPEC.md、FEATURES.json、experiment.toml 和 TASKS.md 的当前阶段。SOURCES.md 是资料索引，不是新的一层权威。只有一份当前研究规格，不新增宪法、权限平台或证明系统。

## 做什么
建设本地、日频、SVXY/CASH 的联合研究程序：真实数据→联合特征→滚动预测→真实时钟持仓回测→中文报告。LLM 负责开发和研究协助，不在每天临场解释后改权重。

本轮数据请求与研究（含特征预热、训练和目标）统一从 2019-01-01 起；这是用户因 2018 年 SVXY 目标倍数变更而确认的选择。边界与首次预测条件见 RESEARCH_SPEC.md 和 experiment.toml，变更记录见 runs/p1/experiment_change.json。

## 怎么做
- 每轮只完成用户指定的一个阶段。先给不超过五点的执行计划，然后自主完成常规实现、运行和修复；阶段完成后停下让用户验收。
- 先检查现有 Python 与依赖；优先成熟库。用一个 Python 包、一个实验配置、一套普通测试、本地 CSV/JSON 和 HTML 报告。首版不建数据库服务、Web 后端、Docker、自治 Agent 平台或实时行情系统。
- 本地单写入线程。可让只读子智能体核对资料/时钟/计算；不让多个代理同时改同一工作区。
- 模型、公式、训练窗口、时点、成本、样本区间和指标调整必须写入实验记录，不能为改善结果而悄悄改目标。没有优势也要交付真实结果。
- 保留原始数据与来源、下载时间、摘要。数据抓取失败不合成行情、不改成代理冒充真实 VX。模拟数据只允许在明确标记的单元测试夹具中使用。
- 标准化、样条节点、参数选择都只在当时训练样本拟合；标签必须成熟。不可把概率、历史分位、模型分数混称。
- 基金真实收益已包含的费用不能重复扣。回测按下一交易日收盘及明确成本代理执行，不得同日收盘偷跑。
- 网络重试有界。失败后定位 DNS/权限/HTTP/许可/字段层面的确切原因，不无限换数据源。
- 不读交易账户，不发订单，不上传付费原始行情，不打印密钥；不自动购买或改系统级安全设置。
- 新增规则只针对实际重复错误。不要写测试锁定散文措辞，不用降低断言来掩盖计算错误。

## 交付与进度
每阶段交付真实可打开的报告、实际运行命令和结果、最重要的未解决项。更新 STATUS.md。分开写“工程完成/未完成”“研究结果”“真实数据覆盖”，不使用一个 PASS 代替三件事。
完成后请用户验收；验收后再保存本地 Git 检查点。远端 GitHub、PR、云运行不作为首版前置。
断线后读 STATUS.md 和现有文件继续，不重新 clone、不重新建架构、不删除既有原始数据。

## 命令
以下命令在 `/Users/logan/SVXYLab` 执行。复用本机 Python 3.12.13 和已有 `.venv`；不重复建环境。唯一项目入口为 `.venv/bin/python -m svxylab`。

P1 数据复算与报告：
```sh
.venv/bin/python -m svxylab data
```
P1 历史运行退出码 0；核心共同样本 1930 日，冻结至 2026-09-04，当时普通 pytest 18 项通过。macOS 打开报告命令返回 0；当时的工具锁屏记录保留。用户现已明确验收 P1，检查点为 `dc3d4ff`。每次运行记录与 pytest XML 在 `runs/p1/<UTC时间>/`。优先验证并复用原件缓存，不刷新冻结截止日。

P2 总回报、时钟、标签与连续账本：
```sh
.venv/bin/python -m svxylab baselines
```
2026-09-07 实际退出码 0，普通 pytest 38 项通过；六个对照各 1930 行，五日标签 1924 行完整、6 行尚未成熟。加 `--open` 生成后由 macOS 打开 `reports/baselines.html`。只读取 P1 冻结文件，不调用取数或模型；每次 CSV/图保存在 `data/clean/p2/<UTC时间>/`，运行 JSON 与测试结果在 `runs/p2/<UTC时间>/`，已有结果不覆盖。页面净值与汇总展示 2019–2023；2024 年后仅用于公司行动核账和时钟示例。

P3 原始联合特征与信息重叠：
```sh
.venv/bin/python -m svxylab features --open
```
仅读取冻结 P1 文件、已验收 P2 总回报与时钟；不读标签进行特征计算，不取数或拟合。19 项核心 CSV 与两项 SKEW 扩展 CSV 分开保存，保留每个交易日及缺失。当前核心完整 1909 日，核心加扩展共同完整 1905 日，首个完整日均为 2019-02-01。2026-09-07 首次运行退出 0，普通 pytest 69 项通过；每次输出在 `data/clean/p3/<UTC时间>/`，命令/测试/审计在 `runs/p3/<UTC时间>/`。报告为 `reports/features.html`，相关性仅展示 2019–2023；公式、窗口与展示记录见 `runs/p3/experiment_record.json`。

独立普通测试：
```sh
.venv/bin/python -m pytest -q
```
合成夹具只检查复利算术、日期/合约选择、字段/公司行动解析、账本、特征公式、训练时钟和预测计算，不是真实回测；真实数据核对另见各阶段报告。

P4 受约束联合预测：
```sh
.venv/bin/python -m svxylab predictions --pilot
.venv/bin/python -m svxylab predictions --open
```
先在首个合资格月试运行，再重放决策日 2020–2023。M0/M1/M2 共用同一批完整成熟训练行；每月拟合、每年三个时间块内层选择有限参数。首个预测信息日 2020-09-10、决策日 2020-09-11，此前 175 个请求决策日没有预测。首次完整运行退出 0、普通 pytest 83 项通过：831 个预测日、120 份月度模型快照、4 次年度选参；826 日的目标在 2024 年前结束，可评分。未读取封存段标签值用于拟合或评分，未实现 P5 仓位映射。

报告为 `reports/predictions.html`；每次 CSV、模型/变换 JSON 和图保存在新的 `data/clean/p4/<UTC时间>/`，实际命令、测试、代码及输入摘要保存在 `runs/p4/<UTC时间>/`。计算前口径记录在 `runs/p4/experiment_record.json`。独立数值审计脚本为 `runs/p4/independent_validation.py`；真实输入上的合成未来扰动 pytest 为 `runs/p4/test_real_causality.py`，实际结果和复算对照见 `runs/p4/reproducibility.json`。后者的合成行情只存在明确标记的测试夹具目录中，原始数据保持不变。日常使用唯一项目入口即可。

P4 审查收尾（已验收）：
```sh
.venv/bin/python -m svxylab predictions --review
```
审查参考为 `0810f1a5ae371b4a703b8c1d2e098ffb0c13eae6`，用户验收后保存本地检查点 `de8b43d`。2026-09-07 按原参数完整重新拟合，与原逐日原始/发布预测最大差0；保存系数独立重建误差约1.29e-14，二者不能混称。普通测试87项及另行真实输入合成未来扰动1项通过。原24次选择都严格落在最强收缩边界；M2 Q90的六个交互40个月均为精确零。追加等频分组、33阳性行合并11区间、最高概率日期和2022Q1/Q2逐日覆盖只作审查后诊断。记录入口为 `runs/p4/review_latest.json`，新目录不覆盖原产物。冻结验收见 `runs/p4/acceptance.json`，接受的是完整性、可复算和负结果保留，不是模型优势。本地离线包位于 `data/clean/p4_review/`，只含开发段输入且跨2024标签为空；没有再分发授权，不公开上传。包内 `replay.py` 是离线复算脚本，常规项目入口仍如上。当前已配置origin（首次审查重跑即已存在），本轮没有新增/修改或使用该远端；此前“无远端”仅为历史阶段记录。

P5 经济映射与连续净账本：
```sh
.venv/bin/python -m svxylab economics --open
```
仅读取已验收P4预测和冻结P1～P3输入，不重新拟合、不改模型或配置。2026-09-07 两次完整运行均退出0，普通pytest 97项通过；216份账本逐字节一致，独立逐笔核对216个账户、197538行，最大金额差约9.81e-9美元。报告为 `reports/economics.html`；最终输出在 `data/clean/p5/20260907T024118182655Z/`，命令/测试/摘要在 `runs/p5/20260907T024118182655Z/`，计算前口径在 `runs/p5/experiment_record.json`，复算比较在 `runs/p5/reproducibility.json`。每次运行新建目录，保留旧产物。

M0/M1/M2各main/risk-only及6个简单对照完整交付，基础B=.05、单边c=.0005；只做原定单因素敏感性。完整请求账户1006日，前175日模型无服务；共同起点2020-09-11后831日，全部账户同资本、同一实际收盘时点从现金重建。缺预测保持实际份额和现金；P4最后5个跨2024标签的预测仍用于2023账户，不读2024收益。共同起点M2 main净收益104.33%、最大回撤32.98%，固定100%为194.02%、29.18%；三个模型的μ5过滤均降低本段总收益，较差结果保留。用户已验收P5；实现提交6138e42保留，验收检查点a10575c，记录为runs/p5/acceptance.json。

P6 家族消融、两个挑战者与时间块诊断：
```sh
.venv/bin/python -m svxylab diagnostics --open
```
2026-09-07 实际完整运行退出0，普通pytest 109项通过。7项列消融（A～F及B+D+E）、两个挑战者（M3的504-session历史权重、M2+SKEW）及M2原规格/同SKEW样本两个对照实际完成；440份月模型、44次年度选择，未改原P4模型/预测或P5映射。SKEW仅读取冻结官方原件，在P3已验收次交易日可得假设下运行；核心共同起点2020-09-11、SKEW配对起点2020-09-15，各自同资本重建账户。M3只给预测头训练损失加权，变换保持原训练期无权拟合；这一约定在拟合前已记录。

报告为 `reports/diagnostics.html`，最终输出 `data/clean/p6/20260907T032405398008Z/`，实际命令/测试/摘要 `runs/p6/20260907T032405398008Z/diagnostics_run.json`，拟合前记录 `runs/p6/experiment_record.json`。独立保存系数重建9137行预测、1188个候选损失，另行解M3 Ridge加权正规方程、核对72个账户65480行，以及1004000个bootstrap标量抽样/1004个区间。21/63日块各1000次，配对且不压缩缺失日。首次运行仅审计日期反序列化失败，修复及失败记录保留；两次计算161份CSV和506份JSON排除实际计时后精确一致，全部72份账本逐字节相同。

M3相对M2的MSE/对数损失/Pinball变化为-1.42%/+1.08%/+3.39%；SKEW相对相同样本核心M2为+2.29%/+1.09%/+2.15%。部分删列改善条件性指标，全部正负结果保留；列消融不是原始来源的因果证明。用户已验收P6，检查点5cf576e；P6当时没有读取2024以后评价。随后P7经授权执行，主版本仍是原预定M2，不自动晋升。详见 STATUS.md。

P7 固定历史评估与手动日报：
```sh
.venv/bin/python -m svxylab research --open
.venv/bin/python -m svxylab daily --open
```
用户可双击项目内“重新运行研究.command”和“更新日报.command”，两个脚本均已实际退出0。统一报告 `reports/latest.html`，最终历史输出 `data/clean/p7/20260907T041828788697Z/`，运行记录 `runs/p7/20260907T041828788697Z/research_run.json`；121项普通测试通过。P7源码/配置/依赖/入口在首次读取封存数值前冻结，原版本及工程修正保留；当前49项文件清单 `runs/p7/freeze.json` 和源码副本 `runs/p7/frozen_source/`。后续运行会校验此冻结版本，不静默改研究规则。

2024-01-02至2026-09-04仅称“本轮未参与选择的历史评估”。核心672个预测日/667个评分日/24个阳性行合并6区间；SKEW670/665日。原M0/M1/M2、7项列消融、两个挑战者和两个参照均完成，462份月模型、36份年度选择快照；全部比较、单因素敏感性、21/63日各1000次配对抽样及四个完整案例保留。原模型/目标/网格/仓位公式未改；完整/共同账户从相同资本与2024-01-02执行收盘起跑。

264个账户177672行独立方程审计通过，1004000个bootstrap标量抽样复算一致。两次完整计算的378份CSV、523份JSON排除实际计时后相同，264份账本逐字节相同，见 `runs/p7/reproducibility.json`。P7修复旧验收报告路径、休市请求边界、延迟首日所需的已验收P4预测桥接和JSON返回值解包；所有失败记录保留。首日桥接反例和影响日期见 `runs/p7/initialization_counterexample.json`，未改任何预测。

本段M2对M0的MSE/对数损失/Pinball高47.21%/103.03%/12.40%；M2 main净收益−15.88%、最大回撤54.03%，固定50%为+15.99%/24.84%。risk-only与全部较好/较差结果同样交付；不按经济结果宣称P10有效，不因挑战者局部较好晋升。

第一条真实前向预报 `data/clean/forward/predictions/2026-09-08.json` 于2026-09-07 04:21:49 UTC生成，使用9月4日已取得信息，9月8日纽约15:00决策截止/16:00执行代理；未来标签保持空。重复日报保留首次记录；后续成熟结果单独追加。日报不跑全部历史账户，复用本月核心模型；按冻结规则在需要的月份以实际可得成熟标签更新。P1冻结日不刷新，新日数据只在独立本地忽略目录有界取得。核心缺失/过时有门控；推理完成后缺少截止复查的已知问题仍待修复，见审查记录。此次休市缓存运行无网络请求，未来新交易日下载分支仍待实际前向检验；不是收益优势验证。用户已验收P7，记录为 `runs/p7/acceptance.json`。

P0 已验证的环境报告入口：
```sh
.venv/bin/python -m svxylab environment --open
```
历史 P0 运行退出码 0、4 项合成测试通过；原报告和 `runs/p0/` 保留。今后再运行会检查当前环境，并引用最新 P1 覆盖记录，不用文件数量推定行情覆盖。

依赖锁定在 `pyproject.toml` 和实际版本快照 `requirements-lock.txt`；P2 在同一虚拟环境加入 Matplotlib 3.11.1，P4 加入 scikit-learn 1.9.0、SciPy 1.18.1 及其依赖。使用 macOS arm64 二进制包，没有系统级安装或重建环境；网络/安装结果在 `runs/p4/dependency_probe.json` 和 `runs/p4/dependency_install.json`。已成功执行的本地包元数据刷新命令：
```sh
.venv/bin/python -m pip --isolated --disable-pip-version-check install --no-build-isolation --no-deps --no-index -e .
```
原始安装、失败及成功结果在 `runs/p0/setup.json`、`runs/p1/dependency_install.json`、`runs/p1/yfinance_install_failure.json`、`runs/p1/yfinance_install_cached.json` 和 `runs/p1/editable_refresh.json`。

正式 ETF 仅用 Yahoo Chart 原件；`provider_*` 为供应商值，OHLCV 当时份额复原字段有明确标记。保留 Nasdaq/QC 参考差异以及 P2 的 SPY 分红总回报与供应商复权参考差异。P7已验收，停在本阶段。P0 检查点 `f3cc00f`、P1 检查点 `dc3d4ff`、P2 检查点 `5912f3b`、P3 检查点 `541178b`、P4 检查点 `de8b43d`、P5 验收检查点 `a10575c`、P6 检查点 `5cf576e`；P7实现及审查提交 `840a731` 已推送origin/main，验收另存本地检查点，未改全局Git配置。验收只代表交付完整、真实与可复算，不代表模型有收益优势；已知待修项继续保留。M1/M2在当前开发段的主要平均预测损失未优于M0，原结果保留。
