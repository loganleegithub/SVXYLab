# 分阶段施工提示词

用户每次只发送一个阶段。P0 首次可先用 /plan 核对，批准后退出 Plan 执行；以后同一阶段内的常规安装/修复/测试由 Codex 自主完成。

## P0｜建立可运行的本地工作区

**用户看到：** 环境检查页、一个实际可执行入口和纯算术测试。不是市场研究结果。

```text
读取 AGENTS.md、RESEARCH_SPEC.md、FEATURES.json、experiment.toml、SOURCES.md。这里只是规格包，我没有现成因子库，也没有已验收的旧系统。

本轮只做 P0：在当前文件夹建立最小可运行的 Python 项目。
先用不超过五点列出执行计划，然后自行检查本机 CPU 架构、macOS、可用 Python、Git、网络与依赖。复用兼容环境；需要系统安装、付费或扩大目录权限时只提出一个明确请求，不要让我手写代码或同时安装多套工具。

建立一个 Python 包、项目级虚拟环境、唯一命令入口和普通 pytest。补充 AGENTS.md 的实际运行/测试命令。初始化本地 Git，但不连接远端、不碰其他项目、不改全局 Git 配置。

写一个明确标为合成测试的每日 -0.5x 复利算术测试：指数 100→120→100，对应理想化产品 100→90→97.5。这只是检查运算，绝不能写成真实回测。

生成并实际打开 reports/environment.html，列出实际软件版本、网络检查、已运行命令及结果、缺少什么。当前没有真实行情就写“尚未取得”，不要做漂亮假净值图。不要先写模型或 Web 服务。

更新 STATUS.md。最后只告诉我怎样打开报告、这一阶段是否完成、唯一真正阻塞项是什么。停在 P0，等待我的阶段验收。
```

## P1｜取得并核对真实数据

**用户看到：** 真实数据覆盖表、原始记录抽查和缺口。没有完整覆盖就明说。

```text
读取当前 STATUS.md 和研究规格。本轮只做 P1，取得本项目要求的真实数据，不做收益优化。

首先用 SOURCES.md 的官方入口验证：VIX/VVIX/VIX9D/VIX3M、标准月度 VX 逐合约历史，以及 SVXY/SPY 原始日线、拆分分红。SKEW 能取得就记录，不能取得不阻塞核心。请求 2019 年至最近共同完成交易日，但只报告实际取得的范围。

先下载每类小样本，核对字段、合约身份、到期日、单位、许可和实际响应，再批量取数并缓存。每个源最多做有界重试；明确区分 DNS、授权、HTTP、字段和数据缺口。不能用 VIX3M/VIX 替代 VX，不能生成模拟行情，不得为凑覆盖跳过危机。

ETF 正式数据接口优先核实 Alpha Vantage TIME_SERIES_DAILY_ADJUSTED 或我已有的合法数据；不假定免费、不假定我已订阅。需要购买时，先确认 SVXY/SPY 所需历史/字段/权限，再给我唯一明确的购买或授权决定。不要读取账户资产、不要把密钥放进聊天/日志/Git。

保存原始文件、来源、下载时间和数据摘要。按真实合约日历选择 F1/F2/F3，不把缺失前月用次月冒充。报告历史发布时间无法验证的部分，只声明合理延迟假设，不伪造 PIT。

生成并打开 reports/data.html：每个数据集起止日、应有/实有/缺失行数、核心共同样本、缺失日期、三个正常/压力时点抽查、一个真实换月边界、一个真实公司行动核对。没有证据的项显示未完成。

冻结实际数据截至日，更新 STATUS.md。本阶段结束时仍不训练模型；数据不足时可以交付诚实的数据报告，但不能宣布完整研究数据已经准备好。
```

## P2｜先算对 SVXY、时钟和持仓账本

**用户看到：** 无需机器学习的对照净值、损益流水、日期与标签示例。

```text
本轮只做 P2：用已核对的真实数据建立 SVXY 总回报、标签与连续持仓账本。不要引入机器学习。

严格依照研究规格：只在新 -0.5x 制度内建立 SVXY 目标与自身回看特征；数据 t→t+1 收盘前 60 分钟决策→t+1 收盘加成本执行；五日标签是 t+1 收盘到 t+6 收盘。生成 R5、L5、Y10 三个标签和成熟时间。L5 是入场点到未来每日收盘的最差损失，不是盘中或峰谷回撤。

实现现金、固定 25/50/75/100% SVXY、F2>F1 持有否则现金等对照。主现金收益为 0，费用按预定值和实际换手扣，不能重复扣基金内费用。账户只有一条连续净值，不把五日重叠标签当作每天新增满仓交易。

写可手算测试：拆分不制造收益、分红不消失、执行前旧仓位承担损益、费用来自实际换手、未来信息不影响历史输出、缺少特征时仍记当日已持有资产的损益。

生成并打开 reports/baselines.html 和逐日账户 CSV，给我三段十行以内的中文手算示例：普通行情、换仓、公司行动/压力日。每行说明旧仓位、当日回报、成本、新仓位、期末资产。最后列出实际通过/失败的测试。

更新 STATUS.md，等待 P2 验收。不能用“测试全部通过”替代真实账本核对。
```

## P3｜建立联合信息表

**用户看到：** 19 项真实特征及解释、覆盖与信息重叠；不筛独立赚钱因子。

```text
本轮只做 P3：严格按 FEATURES.json 计算六个核心家族的 19 项特征。可同时建立 SKEW 扩展两列，但不能把它们混进核心版后只报一个共同样本。

逐项保留输入字段、公式、单位、对应交易日、可用性与来源。A04 用同一真实合约的五日变动；A/B 两种期限结构不能混同；E01 明确为隐含与历史方差差，不称已测得真实 VRP。

生成特征覆盖/缺失、原始数值和相关性诊断。相关性与单独收益不作为准入淘汰门槛。不要扩展 RSI/EMA 参数网格，不做单因子显著性选美，不额外加入 GEX、新闻和宏观模型。

选三个真实不同市场日期生成中文状态卡，说明“高压力但正在缓解”“有利曲线但压力加速”等事实如何同时成立。这里是描述，不允许写成模型已经预测成功。

生成并打开 reports/features.html、19 列核心特征 CSV、另列的扩展数据 CSV。测试每个公式和 t+1 决策时钟；篡改未来行情不得改变较早日期的特征。

更新 STATUS.md，等待 P3 验收。
```

## P4｜实现受约束联合预测

**用户看到：** M0/M1/M2 的真实逐期预测与概率/分位评估。

```text
本轮只做 P4：按研究规格实现 M0 无特征基准、M1 联合线性、M2 样条主效应加六个交互；三个目标分别为 μ5、Q90(L5)、P10。使用成熟库和 experiment.toml 的有限网格。

先在一个历史月试运行，记录耗时，再完成 2020～2023 的历史开发段；不查看或按 2024 以后的结果调参。实际训练样本不足就报告首个可预测日期，不降低门槛去覆盖某次危机。

标准化、样条节点、交互变换与超参数都只使用当时训练数据。月度重估系数、年度内层选择参数，严格剔除未成熟或跨验证边界的标签。不使用随机时间拆分、SMOTE 或按账户夏普挑系数。

保存每次拟合的训练截止、行数/事件数、参数、预测生成时点和以后成熟的真实结果。允许概率预测不好；禁止换事件阈值和预测期限来挽救结果。

生成并打开 reports/predictions.html：相对 M0 的预测损失、五组概率校准、分位覆盖、按连续时期的结果、M1/M2 差异、交互的条件作用以及超出训练支持范围的情况。不要只给总准确率。

至少执行一次未来扰动测试和一次标签成熟边界测试，并给出实际结果。更新 STATUS.md，等待 P4 验收。工程完成不要求预测优于 M0。
```

## P5｜完成经济翻译与净收益比较

**用户看到：** 同一时钟下联合策略、风险控制器和简单对照的完整净账本。

```text
本轮只做 P5：把 P4 的时间顺序预测接入已经验证的持仓账本，仍只使用 2020～2023 开发诊断段。

按规格执行唯一主映射：Q90(L5) 给研究风险容量，μ5 高于固定往返成本筛选才持有；另报 risk-only 控制器。预算 5%、单边成本 5bp 是研究默认，不得写成安全仓位或最优值。实际动态交易另逐笔扣成本。

对照现金、25/50/75/100% 固定仓位、简单期限结构规则，以及 M0/M1/M2 的同映射结果。报告平均敞口，防止把单纯少持仓当成择时优势。事后匹配平均敞口只作为诊断，必须标记。

执行预定成本、预算、额外一交易日延迟的单因素敏感性，不做全部组合择优。报告净收益/回撤/最差路径/成本/换手/空仓和错失反弹；不要只看夏普或胜率。

生成并打开 reports/economics.html 和逐日净值/交易账本，挑最有利、最不利各两个真实路径说明预测如何变成盈亏。把“预测有用但翻译无用”“收益过滤拖累”“只有降敞口效果”“未检测到优势”分别说明。

更新 STATUS.md，等待 P5 验收；失败结果也可以完成研究，不得自动修改目标。
```

## P6｜验证交互、信息贡献与更新价值

**用户看到：** 分组消融、两个有限挑战者、明确不确定性。

```text
本轮只做 P6：在开发诊断段完成预先限定的诊断和挑战者，不无限开新模型。

对 A～F 做分组删除后重新训练；相应交互也删除。由于 E 派生自 B/D，另做 B+D+E 联合删除，写清每次是删列还是删原始信息。共同日期配对比较，不把重要性排名当因果证明。

只做两个额外候选：M3=M2 加 504-session 半衰期历史权重；M2+SKEW 两列（仅在真实历史和发布时间假设可核实情况下）。分别比较，不同时改变。缺少 SKEW 就明确延期这一项，核心版继续。

做 21/63 日时间块的不确定性分析，展示结果对时间区间和压力事件的依赖。模型参数或某次危机不能被当成唯一真理。所有试验和失败都保留。

可调用只读子智能体分别审计数据/时间和持仓数学；只有主线程修改代码。审查意见必须带文件/行、可执行反例或复算，不接受纯文案阻断或泛化重构建议。

生成并打开 reports/diagnostics.html，明确下一阶段封存运行的主版本仍是预定 M2，不按封存历史结果晋升模型。更新 STATUS.md，等待 P6 验收。
```

## P7｜封存历史评估与日常可用交付

**用户看到：** 完整报告、两个本地启动入口、未来预测记录。

```text
本轮只做 P7：冻结当前规格、代码与配置，以预先规定的滚动更新程序运行 2024 年至 P1 冻结日的封存历史段。不得先看结果再改规则；允许程序仅消化当时已成熟的过去结果。

如果这段历史以前已经被我或旧项目看过，准确写“本轮未参与选择的历史评估”，不能称绝对未见。完成全部预定比较，结果差也要如实交付。

生成统一中文 reports/latest.html：数据、预报、研究三部分。保留μ5/Q90(L5)/P10各自含义和样本数，研究仓位参考必须带 RESEARCH_ONLY，不用一句AI叙述覆盖数值。

在本机实际生成并验证“更新日报.command”和“重新运行研究.command”两个入口，或同等易用的项目内启动方式；我不需要手写终端命令。日报只执行冻结程序，不搜索新模型，不调用LLM决定权重，不要求每天重新做全部回测。

记录一条当时真实可得信息产生的前向预测及时间；尚未成熟的结果保留为空。之后每个交易日可由我手动运行。几天/二十天的运行只能验证流程，不能证明收益优势。

复查秘密/付费数据没有进入Git，更新实际运行方法与 STATUS.md。给我能打开的报告、本地代码检查点、实际测试结果、最重要研究结论和未解决限制。等待最后验收，不连接账户、不自动下单、不新建云平台。
```

## 阶段验收后继续

```text
我验收的是本阶段交付完整、结果真实且可复算，不代表模型有收益优势。请先保存本地 Git 检查点并更新 STATUS.md，然后仅执行 TASKS.md 中的下一个阶段；保留既有原始数据与失败结果，不重建架构，不连接远端或交易账户。
```

## 只读审查

```text
你是本项目的只读审查员，不负责重写系统。先读当前阶段、研究规格与实际产物，再运行必要的已有测试或独立复算。
只审查：数据真实性与样本覆盖、信息可得时间、标签成熟/重叠、训练变换是否前视、公司行动/基金制度、换仓与费用账本、预测/经济结论是否被夸大。
每个问题给出具体文件位置、实际影响和可运行反例或最小复算。明确区分阻断本阶段的计算错误与可延期研究改进。
不要按散文措辞挑错，不新增治理平台，不要求证明全局最优，不以“可能有未知风险”为由无限阻断。不得修改文件。没有发现阻断问题也照实报告，这不等于认证策略赚钱。
```

## 中断恢复

```text
从当前项目文件继续。先读 STATUS.md、当前阶段产物和最近实际运行记录，确认上次做到哪里。不要重新下载全部数据、重新 clone、重建项目或重做已完成阶段。只恢复尚未完成的部分；本次仅修复能以实际错误说明的阻塞，不扩展目标。
```
